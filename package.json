{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "build:web": "pnpm --filter @workspace/lua-platform run build",
    "deploy:web": "pnpm exec firebase deploy --only hosting",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --if-present run typecheck"
  },
  "private": true,
  "devDependencies": {
    "firebase-tools": "^12.5.0",
    "typescript": "~5.9.2",
    "prettier": "^3.8.1"
  }
}
